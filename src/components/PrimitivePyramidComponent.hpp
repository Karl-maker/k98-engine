#pragma once

/// Drawn with the built-in pyramid mesh + unlit shader path in OpenGLVer2Renderer.
struct PrimitivePyramidComponent {
    float scale = 1.f;
    float color[3] = {1.f, 0.55f, 0.15f};
};
